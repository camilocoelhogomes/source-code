# E2eRun — loop autônomo W1 (pós T28/T29 merge)

| Campo | Valor |
|---|---|
| Feature | `mvp-local-e2e-green` |
| Data/hora (UTC) | `2026-07-19T12:11:00Z` |
| Branch | `main` |
| Commit SHA | `d4aab4b` |
| Comando | `.venv/bin/python -m github_rag.e2e` |
| Duração | ~624s |

## Pré-condições

| Check | Status |
|---|---|
| `.venv` | ok (reutilizado W0) |
| `.env` / token | ok (`E2E_GITHUB_TOKEN`) |
| Podman + compose | ok |
| T28/T29 mergeados | ok (PR #40, #41) |

## Resultado

| Métrica | Valor |
|---|---|
| exit code | `3` |
| fase falha | `healthy` (healthz timeout) |
| robot executado? | **não** |
| mensagem | `healthz timeout: <urlopen error timed out>` |

## Diagnóstico (sanitizado)

| Fase | Resultado |
|---|---|
| compose dev infra | ok |
| host app (`github_rag.delivery`) | **running** (~60s+) |
| `GET /healthz` | **404 Not Found** (Uvicorn log) |
| `GET /` | 200 |
| `GET /docs` | 200 |

Causa raiz: rota `GET /healthz` registrada em `_materialize_surfaces` **após** `StaticFiles` montado em `/` em `create_app`; mount intercepta `/healthz` → 404. Launcher poll 600s nunca vê `status=ok`.

## Classificação

| ID | Superfície | Classificação | Task alvo |
|---|---|---|---|
| F-W1-001 | `health` / boot | `produto` | **T31** (novo) |
| F-W1-002 | `tooling-e2e` | `produto` | efeito colateral de F-W1-001 (launcher timeout) |

## Handoff

→ W2 discovery: abrir **T31**; dedup T22–T30 (não cobre ordem healthz/static).
