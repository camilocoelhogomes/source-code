# Venv bootstrap — loop e2e Robot

| Campo | Valor |
|---|---|
| Data | 2026-07-19 |
| `.venv/` criado? | não (já existia) |
| Deps atualizadas? | não |
| Motivo | `.venv/bin/python -c "import github_rag.e2e"` ok; `pyproject.toml` inalterado desde último install |
| Python | 3.14.6 (venv existente) |
