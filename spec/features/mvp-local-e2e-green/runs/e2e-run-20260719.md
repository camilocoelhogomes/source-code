# E2eRun — prova local MVP (T01)

| Campo | Valor |
|---|---|
| Feature | `mvp-local-e2e-green` |
| Task | `T01-run-e2e-local` |
| Data/hora (UTC) | `2026-07-19T11:43:58Z` (run B; run A `2026-07-19T11:35:04Z`) |
| Branch | `main` |
| Commit SHA | `c339988` |
| Python | `Python 3.14.6` |
| OS | `Darwin 24.6.0 arm64` |
| Podman | `podman 6.0.1`; machine `Currently running` |
| Comando | `python -m github_rag.e2e` (env via loader Python seguro — `.env` com `INDEX_CRON` quebra `source`) |
| Repo ref | `camilocoelhogomes/source-code` |

## Pré-condições

| Check | Status |
|---|---|
| `.env` local | ok (não versionado) |
| Token | `present=true` (`E2E_GITHUB_TOKEN`; valor não registrado) |
| Podman + compose | ok (`podman-compose` no PATH) |
| deps e2e + rfbrowser | ok |

## Resultado agregado

| Métrica | Valor |
|---|---|
| exit code | `3` (esperado — `E2eStackError` healthz timeout) |
| robot executado? | **não** (falha na fase healthy) |
| duração run B | ~10m+ (interrompido manualmente após timeout healthz) |

## Attempt A — `source .env` quebrado

| Campo | Valor |
|---|---|
| Motivo shell | `.env:8: command not found: 2` — `INDEX_CRON=0 2 * * *` sem aspas |
| Consequência | token/`GITHUB_TOKEN` não exportados; delivery falha com header inválido |
| Superfície | `tooling-e2e` |
| Classificação | `tooling-e2e` + `flakiness` operacional |

## Attempt B — compose sobe; delivery morre no boot

| Fase | Resultado |
|---|---|
| compose dev infra | **ok** — postgres healthy, qdrant, zoekt, slm up |
| host app (`github_rag.delivery`) | **fail** — exit 1 durante `catalog_sync` |
| `/healthz` | nunca respondeu |
| Robot | não iniciou |

### Evidência delivery (sanitizada)

```
github.GithubException.UnknownObjectException: 404 ... get-an-organization
GitHubDiscoveryError: erro HTTP 404 ao listar repositórios da org 'camilocoelhogomes'
CatalogSyncError: falha na descoberta GitHub durante sync do catálogo
```

Causa raiz: `e2e/fixtures/config.e2e.json` usa `"orgs": ["camilocoelhogomes"]`, mas `camilocoelhogomes` é **conta de usuário**, não organização GitHub.

### Evidência launcher (sanitizada)

- Processo filho `github_rag.delivery` vira `<defunct>` (zombie).
- Launcher permanece em poll `/healthz` até timeout (~600s) sem fail-fast.
- Superfície: `tooling-e2e` | Classificação: `produto` (observabilidade e2e)

## Mapeamento por superfície (handoff T02)

| ID | Superfície | Classificação | Task alvo |
|---|---|---|---|
| F-T01-001 | `tooling-e2e` | `tooling-e2e` | **T29** (novo) |
| F-T01-002 | `catalog_indexing` / boot | `produto` | **T28** (novo) |
| F-T01-003 | `tooling-e2e` / health | `produto` | **T29** (fail-fast launcher) |
| — | gaps auditoria | `gap-teste` | **T22–T27** (pendentes; incluir no orquestrador) |

## Handoff

→ T02 (`open-bug-tasks-parent`): abrir/atualizar tasks pai; dedup T22–T27.
