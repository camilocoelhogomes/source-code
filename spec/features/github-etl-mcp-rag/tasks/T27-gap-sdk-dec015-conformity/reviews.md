# Reviews — T27-gap-sdk-dec015-conformity

| Campo | Valor |
|---|---|
| Feature | `github-etl-mcp-rag` |
| Task | `T27-gap-sdk-dec015-conformity` |

## 0. Histórico

| Data | Autor | Decisão | Versão | Observações |
|---|---|---|---|---|
| 2026-07-19 | QA Engineer | `TESTS_READY_FOR_REVIEW` | `0.1.0` | Unitários prontos para gate Architect. Ver `unit-test-plan.md` 0.1.0. Suíte nova `tests/bdd/test_dec015_conformity.py` + `tests/bdd/support/dec015_pins.py` (I-T27-001) + promoção `tests/unit/ui/helpers.py` (I-T27-002) + refactors comportamentalmente neutros de `TestCD05SdkManifest`/`TestUiImports`. RED nomeado e íntegro em `TestDEC01PinsVersionMatrix::test_all_dec015_packages_declare_a_version_range` (`alembic`, `psycopg[binary]` sem faixa de versão no `pyproject.toml` — gap real, fora de `src/github_rag/**`). Restante da suíte nova já verde nesta etapa porque a produção (T05/T08/T10/T15/T17/T18/T20) já usa os SDKs oficiais — consolidação de evidência, não implementação nova (design §2/§8). Full suite: 1 falhado (o RED nomeado) / 1253 passados / 2 pulados (pré-existentes, sem Docker); cobertura global 96.53% (gate ≥95%). Corner case observado e não corrigido (fora de escopo QA): `tests/bdd/test_local_discovery_git_sdk.py`/`test_dec015_conformity.py::TestDEC03GitPythonReference` falha por `ImportError` de import circular pré-existente (`github_rag.catalog.sync` ↔ `github_rag.sources.local.discovery`) **somente quando executado de forma isolada**; passa na suíte completa (ordem de import diferente). Não é regressão desta task — reportar ao Architect para avaliar se abre task de hardening separada. Aguardando review do Architect antes de qualquer implementação "verde" (fechar o RED de `pyproject.toml`) pelo Developer. |
| 2026-07-19 | tech-lead-architect | `CHANGES_REQUIRED` | 0.1.0 | Review dos unitários/BDD da etapa QA (commit `54873ab`). Ver achados §1 abaixo. Evidência QA reproduzida integralmente nesta review (venv efêmera, removida ao final): suíte isolada `python -m pytest tests/bdd/test_dec015_conformity.py -q --no-cov` → **2 failed, 37 passed, 7 subtests passed** (`TestDEC01PinsVersionMatrix::test_all_dec015_packages_declare_a_version_range` citando `alembic`/`psycopg[binary]`; `TestDEC03GitPythonReference::test_local_discovery_git_sdk_suite_is_importable` por `ImportError` de import circular); suíte completa `python -m pytest -q` → **1 failed, 1253 passed, 2 skipped**, cobertura `96.53%` (gate ≥95% ok). Confirmado por reprodução direta (não apenas aceito por relato) que o ciclo de import é 100% pré-existente e não é regressão de T27: `pytest tests/bdd/test_local_discovery_git_sdk.py -q` sozinho, sem carregar `test_dec015_conformity.py`, já falha na coleta com o mesmo `ImportError`. Refactors `TestCD05SdkManifest`/`TestUiImports` confirmados comportamentalmente neutros (5/5 passam isoladamente). Pins/regex de `DEC015_VERSION_CONSTRAINTS` verificados um a um contra o `pyproject.toml` real: único par que falha é `alembic`/`psycopg[binary]` — RED preciso, não assert-fraco. Dois achados `MAJOR` de lacuna plano↔teste nos módulos de suporte novos/promovidos (não bloqueiam o RED de `pyproject.toml`, mas bloqueiam a aprovação desta etapa); ver detalhe em §1. |
| 2026-07-19 | QA Engineer | `TESTS_READY_FOR_REVIEW` | `0.2.0` | R-1/R-2/R-3 corrigidos (ver §2). RED de `alembic`/`psycopg[binary]` (DEC015-01) preservado íntegro. Nenhum código de produção alterado; nenhum assert enfraquecido. Aguardando nova review do Architect. |
| 2026-07-19 | tech-lead-architect | `APPROVED_BY_ARCHITECT` | 0.2.0 | Re-review do commit `87c944e`. R-1/R-2/R-3 verificados por reprodução direta (venv efêmera, removida ao final), não apenas por leitura do diff: `pytest tests/bdd/test_dec015_conformity.py::TestDec015PinsHelperContract tests/unit/ui/test_imports.py -v --no-cov` → **7 passed** (3 novos de R-1/R-2 + 1 novo de R-3 + 3 pré-existentes de `TestUiImports`). Suíte completa `python -m pytest -q` → **1 failed** (`TestDEC01PinsVersionMatrix::test_all_dec015_packages_declare_a_version_range`, RED de DEC015-01 preservado intacto e íntegro) / **1257 passed** (+4 em relação à rodada anterior, exatamente os novos testes) / 2 skipped; cobertura global `96.53%` (gate ≥95% mantido). Diff (`git show 87c944e`) confere: só adições em `tests/bdd/test_dec015_conformity.py`/`tests/unit/ui/test_imports.py` + docs — nenhum assert existente foi tocado/enfraquecido, nenhum arquivo em `src/github_rag/**` alterado. `unit-test-plan.md`/`approvals.md` atualizados para `APPROVED_BY_ARCHITECT` v0.2.0. Achados R-4/R-5 (`SUGGESTION`) permanecem em aberto, não bloqueiam — ver §3 para handoff Developer. |

## 3. Handoff — Developer

| # | Ação | Detalhe |
|---|---|---|
| D-1 | Fechar o RED nomeado de DEC015-01 | Adicionar faixa de versão em `pyproject.toml` para `alembic` e `psycopg[binary]` (ex.: `"alembic>=1.13"`, `"psycopg[binary]>=3.1"` — confirmar faixa mínima compatível com o que já está instalado/testado no projeto). Alteração fora de `src/github_rag/**`; não deve tocar em nenhum outro pin já verde. Verificar com `pytest tests/bdd/test_dec015_conformity.py::TestDEC01PinsVersionMatrix -q --no-cov` até 4/4 verdes. |
| D-2 | DEC015-14 — sincronizar inventário de auditoria | Atualizar `spec/features/mvp-e2e-audit-hardening/audit/coverage-inventory.md` (linha BDD-024: `lacuna` → `coberto-integral`, citando `tests/bdd/test_dec015_conformity.py` como prova). Ajustar as duas suítes de regressão da feature filha: remover `BDD-024` de `T21_KNOWN_PARTIAL_OR_SMOKE` em `tests/bdd/test_mvp_e2e_audit_coverage_inventory.py` e de `EXPECTED_LACUNA_BDDS` em `tests/bdd/test_mvp_e2e_audit_gap_fill_backlog.py` (design §2.5 / bdd.md DEC015-14 já aprovados). |
| D-3 | Não enfraquecer nenhum assert | Nenhuma regra de `DEC015_INTEGRATION_RULES`/`DEC015_VERSION_CONSTRAINTS` deve ser relaxada para "passar verde" — o único caminho aceito para D-1 é ajustar o `pyproject.toml`, nunca a regex/constraint de teste. Rodar a suíte completa (`python -m pytest -q`) ao final: esperado 0 failed / cobertura ≥95% (baseline atual 96.53%, não deve regredir). |
| D-4 | Fora de escopo (não fazer) | Não tocar em `e2e/robot/**`/composes; não relaxar R-4/R-5 (ficam como débito documentado, não como bloqueio). |

| 2026-07-19 | Developer | `IMPLEMENTATION_READY_FOR_REVIEW` | 0.1.0 | Commit `6e5ebf2`. D-1: `pyproject.toml` — `alembic>=1.13`, `psycopg[binary]>=3` (compatíveis com `alembic 1.18.5`/`psycopg 3.3.4` instalados; `_GENERIC_MIN_VERSION_RE` satisfeito). RED de `TestDEC01PinsVersionMatrix::test_all_dec015_packages_declare_a_version_range` fechado (`pytest tests/bdd/test_dec015_conformity.py -q --no-cov` → só falha remanescente é o corner case pré-existente de import circular isolado, já documentado em R-5, não regressão). D-2: `coverage-inventory.md` BDD-024 → `coberto-integral` (evidencia_pytest cita `tests/bdd/test_dec015_conformity.py` + suítes T10/T11/T13/T19/T20; `nota_parcial_t21`/`motivo_lacuna` = `—`); `BDD-024` removido de `T21_KNOWN_PARTIAL_OR_SMOKE` (`test_mvp_e2e_audit_coverage_inventory.py`) e de `EXPECTED_LACUNA_BDDS` (`test_mvp_e2e_audit_gap_fill_backlog.py`); `DENYLIST_PARTIAL` intocado (design). Regressão pega na própria etapa: `tests/unit/audit/inventory_schema.py` duplicava `T21_KNOWN_PARTIAL_OR_SMOKE` com `BDD-024` (consumido por `test_coverage_inventory_schema.py`, fora da lista de arquivos da missão) — corrigida no mesmo commit para não deixar `pytest -q` quebrado; nenhum assert enfraquecido, só o conjunto de IDs sincronizado com a nova evidência integral. D-3: nenhuma regex/constraint de teste relaxada; nenhum arquivo em `src/github_rag/**`/`e2e/robot/**`/composes tocado. Suíte completa: `1258 passed, 2 skipped`; cobertura global `96.53%` (gate ≥95%). Aguardando review do Architect antes do Blue refactor/PR. |

## 1. Achados

| # | Severidade | Artefato / Evidência | Achado | Correção esperada |
|---|---|---|---|---|
| R-1 | `MAJOR` | `tests/bdd/support/dec015_pins.py:118-134` (`read_pyproject_dependencies`, branch `raise AssertionError` linhas 129/133) e `unit-test-plan.md` §2 (`UT-DEC-P02`) | `unit-test-plan.md` promete cenário "`pyproject.toml` sem `[project].dependencies` → `AssertionError`" para `read_pyproject_dependencies`, mas nenhum teste do repositório exercita esse branch (busca por chamadas com `path=` diferente do `pyproject.toml` real não encontrou nenhuma). Só o caminho feliz é exercitado, indiretamente, via o manifesto real. Função pública nova (I-T27-001), pensada para reuso — o branch de erro não tem nenhum teste-oráculo. | Adicionar teste em `tests/bdd/test_dec015_conformity.py` (ou módulo de teste dedicado a `dec015_pins`) que grava um `pyproject.toml` fixture via `tempfile`/`tmp_path` sem `[project].dependencies` (ou com a chave vazia) e assere `AssertionError` de `read_pyproject_dependencies(path=fixture)` — mesmo padrão já usado em `TestSourceConformityRuleHelperContract` (linhas 676-733) para os helpers `assert_source_conforms`. |
| R-2 | `MAJOR` | `tests/bdd/support/dec015_pins.py:150-167` (`dependency_spec`, branch `raise AssertionError` linha 167) e `unit-test-plan.md` §2 (`UT-DEC-P07`) | Mesma lacuna do R-1 para `dependency_spec`: o cenário "nome ausente em `deps` → levanta" está no plano (`UT-DEC-P07`) mas não há teste chamando `dependency_spec` com um nome que não existe na lista — todas as chamadas atuais (`test_dec015_conformity.py:296,475,580`) usam nomes presentes no `pyproject.toml` real. | Adicionar teste unitário chamando `dependency_spec("pacote-inexistente-fixture", deps)` (com `deps` sintético ou o real) e assere `AssertionError`. |
| R-3 | `MAJOR` | `tests/unit/ui/helpers.py:44` (`imports_of_file`, `Path.read_text`/`ast.parse` sem tratamento) e `unit-test-plan.md` §3 (`UT-DEC-R03`) | `imports_of_file` foi promovida a helper público (I-T27-002) e o plano promete o corner case "arquivo inexistente → `FileNotFoundError`/`OSError` propagada (sem swallow)" (`UT-DEC-R03`), mas nem `tests/unit/ui/test_imports.py` nem `tests/bdd/test_dec015_conformity.py::TestDEC09FastApiBinding` chamam a função com um `Path` inexistente. Todas as chamadas atuais usam arquivos reais do pacote `ui`. | Adicionar teste em `tests/unit/ui/test_imports.py` chamando `imports_of_file(Path("/no/such/arquivo-t27-fixture.py"))` dentro de `self.assertRaises((FileNotFoundError, OSError))`, espelhando `TestSourceConformityRuleHelperContract.test_missing_module_path_raises` já existente em `test_dec015_conformity.py` (linhas 715-722). |

| # | Severidade | Artefato / Evidência | Achado | Observação |
|---|---|---|---|---|
| R-4 | `SUGGESTION` | `interfaces.md` §5 (tabela classe↔mecanismo) vs. `tests/bdd/test_dec015_conformity.py:527-581` (`TestDEC10Br024Postgres`) | `interfaces.md` documenta `TestDEC10Br024Postgres` como consumidor de `SourceConformityRule`/`assert_source_conforms`; a implementação usa `Path.read_text` + `assertRegex`/`assertNotRegex` diretamente, sem passar pelo helper declarativo. Funcionalmente correto e verificado (todas as 5 asserções reproduzidas e verdes nesta review) — é só divergência de rastreabilidade doc↔código, não um defeito. | Não bloqueia. Atualizar `interfaces.md` §5 na etapa Blue para refletir a implementação real (BR-024 toca 4 arquivos com regras heterogêneas — motivo razoável para não forçar `SourceConformityRule`), ou documentar a exceção no docstring da classe. |
| R-5 | `SUGGESTION` | `tests/bdd/test_local_discovery_git_sdk.py` (falha isolada) / `reviews.md` linha 12 (nota QA) | Reproduzido nesta review: o import circular (`github_rag.catalog.sync` ↔ `github_rag.sources.local.discovery`) é 100% pré-existente — falha mesmo coletando **somente** `test_local_discovery_git_sdk.py`, sem qualquer código desta task carregado. Não é regressão de T27; passa 100% na suíte completa. | Não bloqueia. Concordo com a recomendação da QA: registrar como candidato a task de hardening separada (fora do escopo de T27), não corrigir aqui. |

## 2. Correções R-1/R-2/R-3 (QA — rodada 2)

| # | Correção aplicada | Evidência |
|---|---|---|
| R-1 | `TestDec015PinsHelperContract::test_read_pyproject_dependencies_raises_when_dependencies_key_missing` e `..._raises_when_dependencies_empty` — fixture `pyproject.toml` sintético via `tempfile.TemporaryDirectory` sem `dependencies`/com `dependencies = []`, assere `AssertionError` de `read_pyproject_dependencies(path=fixture)`. | `PASSED` (ver `commands_run` abaixo). Não usa o manifesto real — exercita o branch de erro isoladamente, sem depender de "quebrar" o `pyproject.toml`. |
| R-2 | `TestDec015PinsHelperContract::test_dependency_spec_raises_when_name_absent` — chama `dependency_spec("pacote-inexistente-fixture", deps_sintetico)` e assere `AssertionError` citando o nome do pacote. | `PASSED`. |
| R-3 | `TestUiImports::test_imports_of_file_raises_for_missing_file` (`tests/unit/ui/test_imports.py`) — chama `imports_of_file(Path("/no/such/arquivo-t27-fixture.py"))` dentro de `assertRaises((FileNotFoundError, OSError))`. | `PASSED`. |

Nenhum assert de `TestDEC01PinsVersionMatrix`/produção foi enfraquecido; `alembic`/`psycopg[binary]` continuam sem pin de versão no `pyproject.toml` (RED de `test_all_dec015_packages_declare_a_version_range` preservado intacto — não é escopo desta correção, fica para o Developer). Suíte afetada (`tests/bdd/test_dec015_conformity.py` + `tests/unit/ui/test_imports.py`): 45 passed / 1 failed (o RED nomeado) / 7 subtests passed. Suíte completa do projeto: 1 failed / **1257 passed** (+4 em relação à rodada anterior, exatamente os 3 testes novos de R-1/R-2 + 1 de R-3) / 2 skipped; cobertura global **96.53%** (gate ≥95% mantido).
