# Refactoring Blue — T31

Sem refatoração estrutural adicional além do builder pattern mínimo (`wire_ui_app` → `DefaultManagementUiApi`; `build(get_state=...)` em runtime).

Baseline: reprodução 404 com mount antes de health (UT-H03c guard).

BLUE_APPROVED_BY_ARCHITECT — 2026-07-19
